<div class="row justify-content-center mt-3">
  <% flash.each do |key, msg| %>
    <div class="col col-md-4">
      <div class="text-center <%= flash_class(key) %>" role="alert" >
        <%= msg %>
      </div>
    </div>
  </div>
<% end %>
