<% flash.each do |name, msg| %>
<div class="row justify-content-center mt-3">
  <div class="col col-md-4">
    <div class="row text-center alert alert-primary" role="alert" >
      <%= content_tag :p, msg, class: "flash #{name}" %>
    </div>
  </div>
</div>
<% end %>
