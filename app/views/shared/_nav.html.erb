
<h1><%= link_to 'Test Guru', root_path %></h1>

<% if logged_in? %>
  <p class="nav user">Welcome, <b><%= current_user.name %></b> Guru</p>

  <%= link_to 'exit', logout_path, method: :delete %>

<% else %>
  <tr>
    <th colspan="2"></th>
    <td><%= link_to 'signup', signup_path %></td>
    <td><%= link_to 'login', login_path %></td>
  </tr>
<% end %>
